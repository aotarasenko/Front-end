import styled from "styled-components";

export const DeleteButton = (props) => {
  return <DeleteButtonStyled>x</DeleteButtonStyled>;
};

export const DeleteButtonStyled = styled.button`
  padding: 10px;
`;
